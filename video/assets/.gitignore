# El original de alta resolución no se versiona; solo el recorte usado
# (que además queda incrustado como base64 en hyfosy.html) se conserva
# como referencia editable.
dra-karina-castillo-original.png
